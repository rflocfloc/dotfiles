
# Add cowsay at bashrc startup if cowsay and fortune are present

which cowsay &> /dev/null;
if [ $? -eq 0 ]; then
    which fortune &> /dev/null;
    if [ $? -eq 0 ]; then
        fortune | cowsay -r
    else
        cowsay "Add fortune package and let me talk!"
    fi
fi
